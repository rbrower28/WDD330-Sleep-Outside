describe("Sanity Check", () => {
  test("Jest is working", () => {
    expect(1).toBe(1);
  });
});

describe('checkoutProcess.js tests', () => {
    // test('getNumberOfItems is working', () => {
    //     testArray = [1, 2, 3]
    //    expect(1).toBe(1);
    // });
 });